name: Deploy to Server

on:
  push:
    branches: [ main ]
  workflow_dispatch:  

jobs:
  deploy:
    name: Deploy Bot
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            # Проверяем существует ли директория проекта
            if [ ! -d "${{ secrets.PROJECT_PATH }}" ]; then
              echo "📁 Создаем директорию проекта..."
              mkdir -p ${{ secrets.PROJECT_PATH }}
              cd ${{ secrets.PROJECT_PATH }}
              
              echo "📥 Первая установка - клонируем репозиторий..."
              git clone https://github.com/${{ github.repository }}.git .
            else
              echo "📂 Директория существует, обновляем..."
              cd ${{ secrets.PROJECT_PATH }}
              
              # Проверяем это ли git репозиторий
              if [ ! -d ".git" ]; then
                echo "⚠️ Не git репозиторий, инициализируем..."
                git init
                git remote add origin https://github.com/${{ github.repository }}.git
                git fetch origin
                git checkout -b main origin/main
              else
                echo "📥 Обновляем код..."
                git fetch origin
                git reset --hard origin/main
              fi
            fi
            
            cd ${{ secrets.PROJECT_PATH }}
            
            # Создаем .env из GitHub Secrets
            echo "📝 Создаем .env файл..."
            cat > .env << 'ENVEOF'
            # Telegram
            TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
            ADMIN_TELEGRAM_IDS=${{ secrets.ADMIN_TELEGRAM_IDS }}
            
            # Database
            POSTGRES_USER=${{ secrets.POSTGRES_USER }}
            POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
            POSTGRES_DB=${{ secrets.POSTGRES_USER }}
            
            # LLM Provider
            LLM_PROVIDER=${{ secrets.LLM_PROVIDER }}
            
            # OpenAI
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            OPENAI_MODEL=${{ secrets.OPENAI_MODEL }}
            OPENAI_EMBEDDING_MODEL=${{ secrets.OPENAI_EMBEDDING_MODEL }}
            
            # Groq (optional)
            GROQ_API_KEY=${{ secrets.GROQ_API_KEY }}
            GROQ_MODEL=${{ secrets.GROQ_MODEL }}
            
            # Bot settings
            CONFIDENCE_THRESHOLD=${{ secrets.CONFIDENCE_THRESHOLD }}
            TAVILY_API_KEY=${{ secrets.TAVILY_API_KEY }}
            ENVEOF
            
            # Проверка наличия .env
            if [ ! -f .env ]; then
              echo "❌ ERROR: Failed to create .env file!"
              exit 1
            fi
            
            echo "✅ .env file created successfully"
            
            # Бэкап базы данных (если контейнер запущен)
            if [ "$(docker ps -q -f name=immigration_bot_db)" ]; then
              echo "📦 Creating database backup..."
              docker-compose exec -T db pg_dump -U ${{ secrets.POSTGRES_USER }} ${{ secrets.POSTGRES_DB }} > backup_$(date +%Y%m%d_%H%M%S).sql 2>/dev/null || echo "⚠️ Backup skipped"
            fi
            
            # Останавливаем контейнеры (БЕЗ удаления volumes!)
            echo "⏹️ Stopping containers..."
            docker-compose down || echo "No containers to stop"
            
            # Создаем директории для данных
            mkdir -p logs backups data
            
            # Проверяем нужна ли миграция векторов
            echo "🔍 Checking if migration needed..."
            NEED_MIGRATION=false
            
            # Проверяем существует ли volume с данными
            if docker volume ls | grep -q "postgres_data"; then
              echo "📊 Database volume exists, checking vector dimensions..."
              
              # Запускаем только БД для проверки
              docker-compose up -d db
              sleep 10
              
              # Проверяем размерность (если таблица существует)
              CURRENT_DIM=$(docker-compose exec -T db psql -U ${{ secrets.POSTGRES_USER }} -d ${{ secrets.POSTGRES_DB }} -t -c "
                SELECT atttypmod 
                FROM pg_attribute 
                WHERE attrelid = 'questions'::regclass 
                AND attname = 'question_embedding'
              " 2>/dev/null | tr -d ' ' || echo "0")
              
              EXPECTED_DIM=1536
              if [ "${{ secrets.LLM_PROVIDER }}" != "openai" ]; then
                EXPECTED_DIM=768
              fi
              
              if [ "$CURRENT_DIM" != "$EXPECTED_DIM" ] && [ "$CURRENT_DIM" != "0" ]; then
                echo "⚠️ Vector dimension mismatch: current=$CURRENT_DIM, expected=$EXPECTED_DIM"
                NEED_MIGRATION=true
              fi
              
              docker-compose down
            fi
            
            # Выполняем миграцию если нужно
            if [ "$NEED_MIGRATION" = true ]; then
              echo "🔧 Running vector dimension migration..."
              docker-compose up -d db
              sleep 10
              echo "yes" | docker-compose run --rm bot python migrate_vector_dimension.py || echo "⚠️ Migration failed or not needed"
              docker-compose down
            fi
            
            # Запуск всех сервисов
            echo "🚀 Starting all services..."
            docker-compose up -d --build
            
            # Ожидание запуска
            echo "⏳ Waiting for services to start..."
            sleep 30
            
            # Проверка Shadowsocks
            echo "🔍 Checking Shadowsocks..."
            if docker logs shadowsocks-client 2>&1 | grep -q "listening"; then
              echo "✅ Shadowsocks is running"
            else
              echo "⚠️ Shadowsocks may not be running properly"
              docker logs shadowsocks-client --tail=20
            fi
            
            # Проверка бота (улучшенная)
            echo "🔍 Checking bot..."
            BOT_LOGS=$(docker logs immigration_bot 2>&1)
            
            if echo "$BOT_LOGS" | grep -q "Бот запущен"; then
              echo "✅ Bot is running successfully"
            elif echo "$BOT_LOGS" | grep -qi "error\|traceback\|exception"; then
              echo "❌ Bot has errors:"
              echo "$BOT_LOGS" | tail -30
            else
              echo "⚠️ Bot status unclear, showing logs:"
              echo "$BOT_LOGS" | tail -30
            fi
            
            # Статус
            echo ""
            echo "✅ Deployment status:"
            docker-compose ps
            
            echo ""
            echo "📋 Full bot logs:"
            docker logs immigration_bot --tail=100
            
      - name: Send Telegram notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: |
            ${{ job.status == 'success' && '✅' || '❌' }} Деплой ${{ job.status == 'success' && 'успешен' || 'провален' }}!
            
            Репозиторий: ${{ github.repository }}
            Коммит: ${{ github.sha }}
            Автор: ${{ github.actor }}
            Ветка: ${{ github.ref_name }}
            
            ${{ job.status == 'success' && '🔐 Shadowsocks прокси активирован' || '⚠️ Проверь логи на сервере' }}